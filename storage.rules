rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permet la lecture publique pour les images
      allow read: if true;
      // Permet l'écriture uniquement aux utilisateurs authentifiés
      allow write: if request.auth != null;
    }
  }
}